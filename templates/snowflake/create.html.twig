{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form)}}
        <div class="form-control">
        {{ form_widget(form) }}
        </div>
        <input type="submit" value="Envoyer">
    {{ form_end (form) }}
{% endblock %}